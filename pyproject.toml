[tool.uv.workspace]
members = ["apps/api", "apps/web", "qa", "watcher"]

[dependency-groups]
dev = ["ruff>=0.8"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
addopts = "-ra"
