{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "required": ["design_markdown", "api_changes", "ui_changes", "test_plan", "acceptance_criteria"],
  "properties": {
    "design_markdown": {
      "type": "string",
      "description": "Technical design: what changes where, data model deltas, error handling. References concrete files."
    },
    "api_changes": { "type": "array", "items": { "type": "string" }, "description": "Endpoint-level changes, empty if none" },
    "ui_changes": { "type": "array", "items": { "type": "string" }, "description": "Page/template-level changes incl. any new data-testid attributes, empty if none" },
    "test_plan": { "type": "array", "items": { "type": "string" }, "description": "Which suites get which new tests" },
    "acceptance_criteria": {
      "type": "array",
      "minItems": 1,
      "description": "FINAL numbered ACs — refined from the spec. These are what QA verifies.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "description"],
        "properties": {
          "id": { "type": "string", "pattern": "^AC-[0-9]+$" },
          "description": { "type": "string" }
        }
      }
    }
  }
}
