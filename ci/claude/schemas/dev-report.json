{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "required": ["summary", "files_changed", "tests_added", "commands_run", "concerns"],
  "properties": {
    "summary": { "type": "string", "description": "What was implemented and how, 3-8 sentences" },
    "files_changed": { "type": "array", "items": { "type": "string" } },
    "tests_added": { "type": "array", "items": { "type": "string" }, "description": "Test node ids or file paths of new/updated tests" },
    "commands_run": { "type": "array", "items": { "type": "string" }, "description": "Verification commands executed and their outcomes, e.g. 'uv run pytest apps/api -q: 14 passed'" },
    "concerns": { "type": "array", "items": { "type": "string" }, "description": "Anything QA or the human reviewer should scrutinize" }
  }
}
